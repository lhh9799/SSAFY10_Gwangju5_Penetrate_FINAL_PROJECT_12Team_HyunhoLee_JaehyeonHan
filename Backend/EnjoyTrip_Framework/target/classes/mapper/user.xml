<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.user.model.mapper.UserMapper">

	<resultMap type="com.ssafy.user.model.UserDto" id="userResultMap">
		<id property="userId" column="user_id" />
		<result property="userName" column="user_name" />
		<result property="userPassword" column="user_password" />
		<result property="emailId" column="email_id" />
		<result property="emailDomain" column="email_domain" />
		<result property="joinDate" column="join_date" />
		<result property="salt" column="salt" />
	</resultMap>

	<insert id="join">
		INSERT INTO members (user_id, user_name, user_password, email_id,
		email_domain, join_date, salt)
		VALUES (#{userId}, #{userName}, #{userPassword}, #{emailId}, #{emailDomain}, now(), #{salt})
	</insert>

	<select id="login" parameterType="Map" resultMap="userResultMap">
		SELECT * FROM members
		WHERE user_id = #{userId} AND user_password = #{userPassword}
	</select>

	<select id="select" resultMap="userResultMap">
		SELECT * FROM members
		WHERE user_id = #{keyword}
	</select>

	<select id="selectSalt" resultType="String">
		SELECT salt FROM members
		WHERE user_id = #{userId}
	</select>

	<update id="chagePassword" parameterType="Map">
		UPDATE members
		SET user_password = #{newPassword}
		WHERE user_id = #{userId}
	</update>

	<!-- <update id="modify" parameterMap="com.ssafy.user.model.UserDto"> -->
	<update id="modify" parameterType="Map">
		UPDATE members
		SET user_name = #{userName}, email_id = #{emailId}, email_domain = #{emailDomain}
		WHERE user_id = #{userId}
	</update>

	<delete id="delete">
		DELETE FROM members
		WHERE user_id = #{userId}
	</delete>


</mapper>
